\documentclass[12pt]{article}

% --- Paquetes esenciales para matemáticas y formato ---
\usepackage[utf8]{inputenc}
\usepackage[spanish]{babel} % Adaptación al español
\usepackage{amsmath, amsfonts, amssymb} % Símbolos y entornos matemáticos
\usepackage{amsthm} % Entornos de teoremas y ejercicios
\usepackage{geometry} % Configuración de márgenes
\usepackage{chngcntr} % --- Definición de entorno para ejercicios ---
\geometry{a4paper, margin=2.5cm}



% --- Paquete para escribir código ---
\usepackage{listings}
\usepackage{xcolor} % Para darle color al código (opcional)

% Configuración opcional para que se vea estilo "caja de código"
\lstset{
  basicstyle=\ttfamily,          % Fuente monoespaciada (Courier / Typewriter)
  backgroundcolor=\color{gray!10},% Fondo gris claro muy sutil
  breaklines=true                 % Permite saltos si el código es muy largo
}



% Define tu entorno de ejercicio
\theoremstyle{definition}
\newtheorem{ejercicio}{Ejercicio}

% Reinicia el contador 'ejercicio' con cada 'subsubsection'
\counterwithin*{ejercicio}{subsubsection}

% --- Datos del documento ---
\title{Apuntes y Ejercicios}
\author{Martín Mateos}
\date{\today}

\begin{document}

\maketitle
\tableofcontents
\newpage
\section{Guia de ayuda para escritura Latex}

Una fórmula matemática en línea se escribe entre signos de pesos: $E = mc^2$. 

Para centrar una ecuación en un bloque independiente:
\[
\int_{a}^{b} f(x) \, dx = F(b) - F(a)
\]



\begin{ejercicio}
Calcule la derivada de la función $f(x) = x^3 + 2x^2 - 5$.
\end{ejercicio}

\begin{ejercicio}
Demuestre la siguiente identidad utilizando las propiedades fundamentales:
\begin{equation}
\sin^2(\theta) + \cos^2(\theta) = 1
\end{equation}
\end{ejercicio}

\section{Repaso de demostraciones}

\subsection{Guía práctica 1}
\section{Introducción a grafos}

\subsection{Guia práctica 2}
hola
\subsection{Algoritmos sobre grafos}
Esta sección define el ordenamiento topológico para luego hacer énfasis encómo recorrer un grafo mediante 
algoritmos, los costos operacionales de estos, las demostraciones formales de los algoritmos y la información
obtenida luego de sus implementaciones. Finalmente,
se verá qué es una arista de corte y cual es su importancia.

\subsubsection{Representación}
Como objeto matemático, un grafo es un par ordenado $G = (V, E) : v,w \in V, \\ (v,w) \in W$. Como estructura
de datos, un grafo puede representarse mediante una lista enlazada o una matriz de dimensión $n\times n$ con
$n = |V(G)|$. \\
Cada estructura debe admitir las siguientes operaciones:
\begin{itemize}
    \item Consultar si $(v,w) \in E$.
    \item Recorrer $N(v)$.
    \item Insertar o remover aristas.
    \item Insertar o remover vértices.
\end{itemize}

Donde \lstinline|adj[v] =| $N(v) = \{ w \in V: (v,w) \in E \}$, es decir el conjunto de vecinos de $v$.

Una implementacion del grafo como lista enlazada de $n$ nodos $V = \{v_1, v_2, ... , v_n \}$ sobre listas 
sería:
\begin{lstlisting}
    Nodo{
        id: Insertar
        ady: List<Nodo>
    }

    Grafo{
        nodos: List<Nodo>
    }
\end{lstlisting}

Entonces cada nodo en la lista hace referencia a sus vecinos, mientras que una implementacion del grafo por
una matriz es simplemente una lista de listas donde el elemnto $ij$ representa que si existe $(v_i, v_j )$.
A continuación dejo las complejidades de cada estructura, con $n=V(D) \land m=A(D)$:

\begin{table}[h!]
    \begin{center}
        \begin{tabular}{l l l }
            Operación   & Lista Enlazada  & Matriz \\ \hline
            Espacio     & $\Theta(n + m)$ & $\Theta(n^2)$ \\
            Consultar $(v,w) \in E(D)$ & $O(d(v))$ & $O(n^2)$ \\
            Recorrer $N(v)$ & $O(d(v))$ & $O(n)$ \\
            Recorrer todas las aristas & $O(n + m)$ & $O(n^2)$ \\ \hline
        \end{tabular}
    \end{center}
\end{table}


Si el grafo posee pesos, estos se almacenan junto con sus vecinos. Para listas \lstinline|adj(v)|=
$<(w_1, p_1), ... , (w_k, p_k)>$ y para la matriz simplemente el valor del peso en el elemento $ij$ teniendo
en cuenta que \lstinline|D[i][j] = null|indica la ausencia de adyacencia.

\subsubsection{Ordenamiento topológico}
Sea $D=(V,E)$ un digrafo. Un \textbf{ordenamiento topológico} de $D$ es un orden lineal $v_1, v_2, ..., v_n \in V(D)$
tal que $v_i \rightarrow v_j \in V(D) \Rightarrow i<j$.

\textbf{Lema:}
\begin{center}
    Todo digrafo acíclico (DAG) tiene un vértice $v$ tal que $d(v)=0$.
\end{center}

\textbf{Teorema:}
\begin{center}
    Un digrafo admite ordenamiento topológico si y sólo si es acíclico.
\end{center}

Demostraciones en apuntes.

\subsubsection{Recorrridos sobre grafos - BFS}
\subsubsection{Recorrridos sobre grafos - DFS}
\subsubsection{Aristas de corte}
\subsubsection{Guía práctica 3}

\begin{ejercicio}
    Para un grafo $G$ el \textbf{conjunto de vencidarios} o conjunto de adyacencias es el par $(V(G), N)$ 
    donde $N$ es la función definida en la sección \textbf{3.2.1} que devuelve el conjunto de vecinos de un 
    vector $v \in V(G)$.

    Suponer que los vértices dados son el conjunto $\{0, 1, ..., |V(G)|-1\}$.
    
    Discutir las ventajas y desventajas en cuanto a la complejidad temporal y espacial de las siguientes 
    implementaciones del conjunto de vencidarios del grafo $G$, de acuerdo a las siguientes operaciones:
    \begin{enumerate}
        \item Inicializar la estructura a partir de una secuencia de vértices y una secuencia de aristas 
        de $G$.
        \item Determinar si dos vértices v y w son adyacentes.
        \item Recorrer y/o procesar el vecindario N (v) de un vértice v dado.
        \item Insertar un vértice $v$ con su conjunto de vecinos $N (v)$.
        \item Insertar una arista $(v, w)$.
        \item Remover un vértice $v$ con todas sus adyacencias.
        \item Remover una arista $(v, w)$.
        \item Mantener un orden de $N (v)$ de acuerdo a algún invariante que permita recorrer cada vecindario
         en un orden dado.
    \end{enumerate}
    \textbf{Estructuras de datos:}
    \begin{enumerate}
        \item La función $N$ se representa con una secuencia (arreglo dinámico o lista enlazada) que en 
        cada posición $v$ tiene el conjunto $N (v)$ implementado a su vez sobre una secuencia (arreglo 
        dinámico o lista enlazada). Cada vértice es una estructura que tiene un índice para acceder en $O(1)$ 
        a $N (v)$. Esta representación se conoce comúnmente como lista de adyacencias.
        \item Ídem anterior, pero cada $w \in N (v)$ se almacena junto con un índice a la posición que ocupa 
        $v$ en $N (w)$. Esta representación también se conoce como lista de adyacencias, pero tiene información 
        para implementar operaciones dinámicas.
        \item $N (v)$ se representa con un arreglo dinámico que en cada posición i tiene un arreglo dinámico de 
        booleanos $A_i$ con $|V (G)|$ posiciones tal que $A_i[j]$ es verdadero si y solo si $i$ es adyacente 
        a $j$. Esta representación se conoce comúnmente como matriz de adyacencias.
        \item $N (v)$ se representa con un arreglo dinámico que en cada posición tiene el conjunto $N (v)$ 
        implementado con una tabla de hash. Esta representación es un mix entre las representaciones clásicas 
        de matriz de adyacencias y lista de adyacencias.
    \end{enumerate}
\end{ejercicio}

\textbf{Respuestas:}

Voy a definir el largo de la secuencia de vectores con $n$ y el largo de la secuencia de aristas con $m$.

\textbf{1.1}) 
\begin{verbatim}
    Es es List<List<Int>>
    Vs es List<Int>


    inicializar(V:Vs, es:Es): list<list<int>>{
        for(i=0, i++, V.len()){
            res.append([])
        }
        forall e in es{  // recorro solo n (las aristas) O(len(n))
            res[es[0]].append(es[1])
            res[es[1]].append(es[0]) 
        }
    }
\end{verbatim}

Para arreglos, encolamos al final $n$ listas vacías ($O(n)$), luego recorremos la vecindad obteniendo las 
aristas $(v,w)$ tal que \lstinline|res[v].append(w)| en cada iteración. Esto es $O(m)$.

En el caso de las listas enlazadas habría que recorrer la lista de vectores ($O(n)$), luego ordenarla para
que el numero de la posición coincida con los vectores ($O(n^2)$). Nos movemos a la secuencia de aristas y 
en la posición v de res colocamos w y viceversa, acceder a un elemento en en la lista enlazada es $O(n)$ y 
como lo hacemos $m$ veces tenermos que la complejidad de almacenar una arista es $O(n \cdot m)$.

Finalmente la complejidad total es $O(n^2 + n \cdot m)$

En cuanto al espacio utilizado, los arrays toman $O(n+m)$ y las listas enlazadas $O(n + m)$, así que por lo
tanto, desempatando por la complejidad temporal me quedo con los arreglos.

\textbf{1.2}
En listas enlazadas buscar a $v$ es $O(n)$ y buscar a $w$ es $O(d(v))$, por lo tanto la complejidad de 
determinar si $w \in N(v)$ es $O(n + d(v))$.

En arreglos dinámicos buscar a $v$ y $w$ es $O(1)$

\begin{ejercicio}
    -Enunciado-
\end{ejercicio}
\textbf{Respuestas para el algoritmo cúbico:}
Para ver mejor el algoritmo intento implementarlo en pseudocódigo, busco vectores conectados
entre sí tal que si $A$ es la matriz de adyacencias entonces $A_{vw} \ A_{wz} \ A_{wv} = 1$.
\begin{verbatim}
    
    for(v=0, v++, A.lenght){ // n op
        for(w=1, v++, A.lenght){ // n-1 ops
            for(z=2, v++, A.lenght){ // n-2 ops
                if (A[v][w]*A[w][z]*A[z][w] == 1){ // 1 ops
                    return true; //1 ops
                }
            }
        }
    }
    
\end{verbatim}

\textbf{a)} El algoritmo es correcto porque explora todas las combinaciones posibles de triángulos.

\textbf{b)} Para responder apropiadamente debo ver el numero de operaciones.
\begin{align*}
    T(n) &=  \sum_{v=0}^{n} \sum_{w=2}^{n} \sum_{z=3}^{n} 2 \\
    &= 2(\sum_{v=1}^{n} \sum_{w=1}^{n-1} \sum_{z=1}^{n-2} 1) \\
    &= 2 (n-2) (\sum_{v=1}^{n} \sum_{w=1}^{n-1}1) \\
    &= (n-2) (n-1)\sum_{v=1}^{n} 1 \\
    &= (n-2) (n-1) n
\end{align*}

Como $T(n) \approx n^3 \land n^3 \in O(n^3) \land n^3\in \Omega(n^3) \implies T(n) \in \Theta(n^3)$ (habría
que demostrar los dos pertenece anterior).

\textbf{c)} El peor caso ya lo mencioné, cuando el triangulo son los últimos 3 nodos. El mejor caso serían
los tres primeros nodos.

\textbf{Respuestas algoritmo cuadrado:}
Si la lista de adyacencias es \lstinline|L:list<list<int>>|, iniciarla es $O(n+m)$:
\begin{verbatim}
    for(v=0, v++, L.lenght){ 
        visitados = []
        for(j=0, w++, L[v].lenght){ // O(n)
            if(pertenece(L[v][j], visitados))
        }
    }
\end{verbatim}

\end{document}

